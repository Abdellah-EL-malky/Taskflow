services:
  - type: web
    name: taskflow-api
    runtime: docker
    rootDir: backend
    buildCommand: mvn clean package -DskipTests
    startCommand: java -jar target/taskflow-0.0.1-SNAPSHOT.jar
    envVars:
      - key: JAVA_VERSION
        value: 17